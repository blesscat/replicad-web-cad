interface:
  display_name: "OpenSpec to Main PR"
  short_description: "Run OpenSpec from exploration through a main PR"
  default_prompt: "Use $openspec-to-main-pr to carry a completed OpenSpec exploration through proposal, implementation review, and a PR to main."
