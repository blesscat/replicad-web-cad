/**
 * Official OpenGrid spacing shared by every grid-based component.
 * Feature-specific offsets and hole dimensions belong to their owning
 * component contracts and must not be added here.
 */
export const OPENGRID_GRID_CONFIGURATION = {
  fullPitch: 28,
  halfPitch: 14,
} as const
