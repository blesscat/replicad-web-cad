# add-snap-remover-step-download

Add the provided `snap remover.step` file as an `opengrid-snap-remover` catalog preview component. Keep its bytes unchanged, route it through the existing CAD Worker, and expose no adjustable parameters in the workspace sidebar.
